import React from 'react';
import './toolbar.css'

const toolBar = (props: any) => (
    <header className="Toolbar">
        <div>MENU</div>
        <div>LOGO</div>
        <nav>
            ...
        </nav>
    </header>
);
export default toolBar;